<tr>
  <td><%= answer.id %></td>
  <td><%= answer.body %><br>
  <%= link_to 'show', admin_answer_path(answer) %> |
  <%= link_to 'edit', edit_admin_answer_path(answer) %> |
  <%= link_to 'delete', admin_answer_path(answer),
          method: :delete,
          data: { confirm: 'Are you sure?' }%>
  </td>
</tr>
