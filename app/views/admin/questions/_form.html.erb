<h1><%= question_header(@question) %></h1>

<%= form_with model: [:admin, @test, @question], local: true do |form| %>
  <%= form.label :body %><br>
  <%= form.text_area :body, id: :question_body %><br>
  <%= form.submit %>
<% end %><br>

<%= link_to '<< Tests', admin_tests_path %>
